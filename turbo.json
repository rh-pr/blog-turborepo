{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"],
      "env": ["SESSION_KEY", "BACKEND_URL"]
    },
    "lint": {},
    "dev": {
        "cache": false
    }
  }
}
