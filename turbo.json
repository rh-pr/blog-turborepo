{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["SESSION_KEY", "BACKEND_URL"]
    },
    "lint": {},
    "dev": {
        "cache": false
    }
  }
}
